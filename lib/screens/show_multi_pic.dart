import 'package:flutter/material.dart';

class ShowMultiPic extends StatelessWidget {
  const ShowMultiPic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
